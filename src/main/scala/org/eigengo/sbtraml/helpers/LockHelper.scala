package org.eigengo.sbtraml.helpers

import com.github.jknack.handlebars.{Options, Helper}

object LockHelper extends Helper[AnyRef] {

  override def apply(context: AnyRef, options: Options): CharSequence = ""

}
